'use server';

export const singIn = async () => {
    try {

    } catch (error) {
        console.log('Error', error)
    }
}
export const signUp = async (userData: SignUpParams) => {
    try {
        // create a user acc
    } catch (error) {
        console.log('Error', error)
    }
}